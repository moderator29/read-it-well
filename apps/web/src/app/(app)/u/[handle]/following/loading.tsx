import { LoadingPeople } from "@/components/social/profile/LoadingPeople";

/** The wait on a following list. Same shape as followers, same component. */
export default function LoadingFollowing() {
  return <LoadingPeople />;
}
